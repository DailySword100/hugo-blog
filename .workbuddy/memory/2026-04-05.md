# 2026-04-05

## Hugo 博客项目修复 (pblog)

- 项目路径：g:\tmp\pblog
- 使用主题：hugo-theme-reimu（D-Sketon 开发）
- 主题文件夹：themes/hugo-theme-reimu

### 修复了两个问题：
1. **主题文件夹名不匹配**：原来克隆的主题文件夹名为 `reimu`，但主题内的 `hugo.toml` 示例写的是 `hugo-theme-reimu`，导致 Hugo 报 "module not found"。修复方法：重命名文件夹为 `hugo-theme-reimu`，并在项目 `hugo.toml` 中将 `theme = 'reimu'` 改为 `theme = 'hugo-theme-reimu'`。

2. **主题文件不完整**：原主题文件夹内缺少 `layouts/` 目录，无法构建。原因是主题没有完整 clone。重新 clone 后问题解决。

### 当前状态：
- `hugo` 构建成功，生成多语言页面（EN/JA/ZH-CN/ZH-TW）
- 主题 `.git` 已删除，避免影响后续推送
- 可以运行 `hugo server` 在 http://localhost:1313/ 预览
